<footer class="bg-gray-800 text-white py-12 mt-16">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <!-- Brand -->
            <div>
                <div class="flex items-center gap-2 mb-4">
                    <svg class="w-8 h-8 text-blue-400" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M4 4h16a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2zm0 2v12h16V6H4zm2 3h12v2H6V9zm0 4h8v2H6v-2z"/>
                    </svg>
                    <span class="text-xl font-bold">PhotoJourney</span>
                </div>
                <p class="text-gray-400">Capturing the world's beauty, one frame at a time.</p>
            </div>

            <!-- Quick Links -->
            <div>
                <h3 class="text-lg font-semibold mb-4">Quick Links</h3>
                <ul class="space-y-2">
                    {% for item in nav %}
                    <li><a href="{{ item.url }}" class="text-gray-400 hover:text-white transition-colors">{{ item.label }}</a></li>
                    {% endfor %}
                </ul>
            </div>

            <!-- Contact -->
            <div>
                <h3 class="text-lg font-semibold mb-4">Contact Us</h3>
                <ul class="space-y-2 text-gray-400">
                    <li>Email: hello@photojourney.com</li>
                    <li>Phone: (555) 123-4567</li>
                    <li>Address: 123 Camera Street, Lens City</li>
                </ul>
            </div>
        </div>

        <div class="border-t border-gray-700 mt-8 pt-8 text-center text-gray-400">
            <p>&copy; 2024 PhotoJourney. All rights reserved.</p>
        </div>
    </div>
</footer>